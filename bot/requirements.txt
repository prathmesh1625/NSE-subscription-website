flask==3.0.3
requests==2.32.3
psycopg2-binary==2.9.9
Pillow>=10.0.0
langchain
langchain-openai
langchain-google-genai
langchain-anthropic
langchain-core
pdfplumber
pypdf
# OCR fallback for scanned filings / newspaper cuttings (output.py). pymupdf
# rasterises the page, pytesseract reads it — the latter also needs the
# tesseract-ocr system package, installed in the Dockerfile.
pymupdf
pytesseract
python-dotenv
