# ── WhatsApp bot (config.py reads these from the environment) ──
# Copy to a local .env / set in your host environment. NEVER commit real values.
WHATSAPP_TOKEN=
PHONE_NUMBER_ID=
VERIFY_TOKEN=

# ── Scraper PostgreSQL DB ──
DB_HOST=localhost
DB_PORT=5433
DB_NAME=nse_ingestion
DB_USER=postgres
DB_PASSWORD=

# ── Branding shown in each Stock Bits alert ──
BRAND_URL=https://equityalerts.in/portal
PUREFRAME_SITE=https://pureframelabs.in/
PUREFRAME_CONTACT=8459625508
